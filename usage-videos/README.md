# usage-videos

Remotion project that renders the cc-plugins dev onboarding video.

## Commands

```sh
npm install
npm run dev        # Remotion Studio preview
npx remotion render  # render to out/MyComp.mp4
```

## Output

`out/MyComp.mp4` — ~107 s walkthrough of scaffold + sdd workflow on the task-tracker example project.
