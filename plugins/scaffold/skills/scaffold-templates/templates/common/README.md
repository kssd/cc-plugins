# {{PROJECT_NAME}}

{{DESCRIPTION}}

## Getting started

```sh
# Install dependencies (run the command for your stack)
# Node:    npm install         (or: pnpm install / yarn install)
# Python:  uv sync             (or: pip install -e ".[dev]")
# Go:      go mod tidy
```

## Development

```sh
# Lint / format
# Node:    npm run lint && npm run format
# Python:  uv run ruff check . && uv run black --check .
# Go:      golangci-lint run

# Test
# Node:    npm test
# Python:  uv run pytest
# Go:      go test ./...
```

## License

{{LICENSE}} — see [LICENSE](./LICENSE).
